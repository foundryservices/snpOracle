[tool.black]
line-length = 150
include = '\.pyi?$'
exclude = '''
/(
    \.git
  | \.hg
  | \.mypy_cache
  | \.tox
  | \.venv
  | _build
  | buck-out
  | build
  | dist
  | docs
  | mining_models
  | contrib
  | scripts
)/
'''

[tool.isort]
profile = "black"
line_length = 150
skip_gitignore = true
skip = [
    ".venv",
    ".git",
    ".mypy_cache",
    "build",
    "dist",
    "docs",
    "mining_models",
    "contrib",
    "scripts",
]

#[tool.flake8]
# Refer to the `.flake8` file
